[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nwsdb-bill-api-docs-unofficial"
version = "0.1.0"
description = "Minimal unofficial HTTP helpers for NWSDB Bill Calculator API (educational)"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[project.urls]
Homepage = "https://github.com/Cookie-Cat21/nwsdb-bill-api-docs"

[tool.setuptools]
packages = ["nwsdb_bill_api_docs"]
